"""PhaseMap — zone-based AMD (Accumulation/Manipulation/Distribution) scanner.

Analysis tool only — no order execution, no financial advice.
All actionable levels are ZONES (price bands), never single prices.
"""

from phasemap.config import CONFIG, PRODUCT_NAME

__all__ = ["CONFIG", "PRODUCT_NAME"]
