[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --strict-markers
markers =
    risk: risk-management and position-sizing rules
    breaker: circuit-breaker conditions
    journal: journal P&L and categorization
    pretrade: pre-trade gate rules
