# Momentum nap audit (2026-09-23)

**Start:** `origin/main` 5cdc3beea79f. Open PRs: **none**.
Branch: `momentum-nap-audit`. The lens is report-only and TABS=5. Nothing on
the kill list was touched, and `journal/` is byte-identical on every commit.

## Q0 — open PRs

None. #19–#32 are all merged (rebase): #19 lens, #20 `?s=`, #21 mode, #22 Pine
box, #23 TF/4H tab, #24 look, #25 notes, #26 crop/box, #27 cards + 4H cap,
#28 why-4, #29 morning audit, #30 hygiene, #31 phone TF bar, #32 TF alignment.

## Q1 — production audit, 390×844

This session's proxy blocks pages.dev (HTTP 403). The audit therefore ran on a
GitHub runner against `https://googy-boys-scanner.pages.dev`, at
2026-09-22T22:59Z, from the throwaway branch `diag-els-4h-2026-09-23`. Results
and screenshots are committed there only. **23/23 automated checks PASS:**

| page | checks | result |
|---|---|---|
| /momentum ASX | no page error; header "4 names · 298 scanned · 867 gated · last bar 2026-09-22"; 4 rows = header; every link `?s=…&m=asx&src=momentum` | PASS |
| /momentum?m=nasdaq | no crash; 20 names · 1157 scanned · 268 gated · last bar 2026-09-21; 20 rows; links | PASS |
| ELS chart, D | chart.js?v=121; SHORT 5.880 / 6.762 / 4.998 / 4.116 / 3.234; 3 panes; "2353 daily bars"; no PhaseMap; no SCORE 0/8; TF bar on screen (729/844) | PASS |
| ELS chart, 4H | "912 4H bars" (not 187); own plan 6.460 / 7.384 ≠ Daily; within $0.02 of TV 6.46 / 7.39 | PASS |
| ELS chart, 3D / W | honest "No Auto box … no scored cross in 60 bars" (785 / 486 bars) | PASS |
| BHP 5.0 chart | legend SMA 10 / 20 / 43 (+200); no Pine caption; 0 Momentum panes; no page error | PASS |

**The screenshots showed a FAIL that the numbers did not.** After a timeframe
switch the 4H chart opened on Oct–Nov 2025 (price axis 1.1–2.4) with the 6.46
box off screen. It reproduces locally on the real bars, where 3D opened on
2021. Fixed in Q2.

## Q2 — fixes (one commit each)

1. **Every Momentum TF switch opened on a stale window.** `applyTF` set the
   live-move view on the price chart only, before the MACD/RSI panes got the
   new timeframe's bars. The panes' Daily range then synced back over it. The
   view now goes on all three charts, last. Browser-checked: 4H, D, 3D and W
   each open on the live move; 4H shows the 6.460 / 7.384 box. +4 pins,
   2 mutations caught.
2. **The empty state dropped `src=momentum`.** `chart.html?src=momentum` with
   no symbol shows "Pick a ticker", and its search navigated without `src`, so
   a Momentum pick opened the 5.0 chart. It now keeps `src=momentum`.
   Browser-checked. +8 pins, 3 mutations caught.

## Q3 — 4H alignment

Not needed. #32 already proved the UTC bucketing from code and data, and
switched ASX to Australia/Sydney 10:00–16:00 and NASDAQ to New York
09:30–16:00. Production reads 6.460 / 7.384 against TV 6.46 / 7.39. Daily is
unmoved.

## Q4 — regression pins

| pin | state |
|---|---|
| fail() / emptyState() keep the Momentum context | **added** (back-link from src; search keeps `src=momentum`) |
| targetBars hourly is range-aware | already pinned: deep_history.test.js (2y/1h ≥ 3000, 1mo/1h 160, other intraday 750) |
| hygiene.test.js red if deleted names return | re-verified by mutation (fetchStockQuote, fmtTurn, priceFor all caught) |
| momentum.js never fetches vivek/spec/phasemap as grades | already pinned; re-verified by mutation (all three injected fetches caught) |
| TABS.length === 5 | already pinned (momentum.test.js) |
| journal_money "per-book equity curves" | **fixed**. It asserted `drawMiniEquity`, which has no caller, so it tested nothing. It now asserts the real `drawEquity(pre + "-equity", series(d.closed), …)` call; removing that call turns it red. Comment in journal.js corrected. `drawMiniEquity` stays (kept list) and is now named by no test. |

## Q5 — crons and data

- `momentum.yml`: own `momentum` concurrency group (not `scan`). Crons are
  post-close: ASX 06:30Z, NASDAQ 21:30Z, crypto 00:30Z. It stages only
  `public/data/momentum/$MARKET.json`, one pathspec, with `assert_staged`. The
  retry loop's `git add -A` runs after a hard reset plus a checkout of this
  run's own file only. Nothing changed.
- **It has only ever run twice, both manual dispatches** (22 Sep 07:19 and
  07:31 UTC). The first scheduled firing (NASDAQ, 21:30Z on 22 Sep) had not
  appeared by ~23:10Z. GitHub's free-tier crons are known to run hours late
  here (CLAUDE.md, MORNING PLAYS: measured 2–5h). Not changed.
- **Coverage:** ASX downloaded **1165 of 2047** (57%, cache `reused: 0`, no
  errors) → 298 scanned, 867 gated, 4 hits. NASDAQ 1425 of 1426 → 1157
  scanned, 268 gated, 20 hits. The ASX shortfall is still the cold-cache
  manual run; the first scheduled ASX run (06:30Z on 23 Sep) is the one to
  read. Yahoo and the gates are unchanged.

## Observed, not changed (outside the Q1 checklist, or owner calls)

- **/momentum filter pills are unstyled.** `momentum.js` renders
  `.deck-pill` / `.deck-pill-n`, and no CSS file defines either. They show as
  grey browser-default buttons reading "ALL4", "RULE A4". The deck uses
  `.fpill`. The one-line fix is to render `.fpill` with a `<b>` count. Not
  applied: the owner ruled "do not restyle Momentum".
- **"NO SPARK" on every row.** The card draws `r.spark`, and neither scan file
  carries a `spark` field. It needs the scanner to emit one (a
  `scanner/momentum` output change), so it was not touched.

## Leftover remote branches (7g) — listed, not deleted (deletes 403 here)

- Throwaway: `diag-els-4h-2026-09-23` (audit and bar-fetch jobs; never merged).
- Merged by rebase, so their SHAs are not on main but the content is:
  `vivek-momentum-v1` (#19), `momentum-chart-link` (#20),
  `momentum-chart-mode` (#21), `momentum-tv-plan` (#22),
  `momentum-chart-agrade` (#23), `momentum-chart-tv-look` (#24),
  `momentum-overnight-2` (#25), `momentum-tv-parity` (#26),
  `momentum-list-parity` (#27), `momentum-drain` (#28),
  `momentum-morning-audit` (#29), `scanner-hygiene-2026-09-23` (#30),
  `momentum-phone-tf-bar-2026-09-23` (#31), `momentum-tf-align` (#32),
  `turtle-job1-cleanup` / `turtle-job2-nav-primary` / `turtle-ui-pass` /
  `turtle-money-readouts` (#13–#17; the lens was later removed).
- Never merged: `claude/optimistic-darwin-r6s49g` (10 commits; the source of
  `tradingview/Final_Top_Script.pine`) and `claude/repository-overview-83qyz6`
  (5 commits, 2026-08-23). The owner decides.
- Fully merged ancestors, safe to delete: `audit-smooth-2026-08-23`,
  `claude/cloudflare-daily-limit-kn6ld1`, `claude/how-you-go-m2wk8c`,
  `claude/mobile-suburb-search-n04zwx`, `claude/stalled-position-surface-8iiegn`,
  `parity-pack`, `turtle-nav-home`.
