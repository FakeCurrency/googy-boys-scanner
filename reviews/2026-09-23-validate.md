# Validate — 2026-09-23 (all-night)

Branch `validate-2026-09-23` off origin/main `cc06a187f`. Kill list untouched
(vivek.py, scan.py, conviction.py, scanner/broker/**, journal/,
confluence_alert.py, morning_plays.py, phasemap/engine, bot_rules.json,
scanner/momentum/screen.py). `journal/` is never staged (tree `7f5420599` at
start).

## V0 — map (09:00Z)

| item | value |
|---|---|
| open PRs | **0** (#19–#37 rebase-merged; #16 closed as a duplicate of #15) |
| last CODE commit | `8501a2449` reviews: sweep land note for #36 (05:04Z) — last product-code touch `2859ce143` styles.css (04:38Z) |
| last DATA commit | `cc06a187f` data: scan 2026-09-23 07:55 UTC |
| momentum.yml | 4 runs, **4 success, 0 failure**. Last success #4 (schedule, crypto) 04:54Z. Crons fire late: NASDAQ 21:30 → 23:42Z, crypto 00:30 → 04:54Z. Today's ASX 06:30Z cron **had not fired at 09:00Z** |
| deployed stamp | `version.json` 2026.09.23-7ef6fd5d |

| file | generated | downloaded | scanned | gated | hits | last_closed_bar |
|---|---|---|---|---|---|---|
| `momentum/asx.json` | 2026-09-22 07:25Z (manual run #1) | **1,165 / 2,047** | 298 | 867 | **4** (ELS IPX BRE LRV) | 2026-09-22 |
| `momentum/nasdaq.json` | 2026-09-22 23:45Z | 1,428 / 1,428 | 1,160 | 268 | 42 | 2026-09-22 |
| `momentum/crypto.json` | 2026-09-23 04:56Z | 60 / 86 | 49 | 11 | 0 | 2026-09-23 |

Crypto momentum file exists: **Y** (0 hits today; 26 of 86 symbols returned no bars).

**Leftover remote branches** (listed, not deleted — deletes 403 from here):
- rebase-merged, content on main: `vivek-momentum-v1` `momentum-chart-link`
  `momentum-chart-mode` `momentum-tv-plan` `momentum-chart-agrade`
  `momentum-chart-tv-look` `momentum-overnight-2` `momentum-tv-parity`
  `momentum-list-parity` `momentum-drain` `momentum-morning-audit`
  `scanner-hygiene-2026-09-23` `momentum-phone-tf-bar-2026-09-23`
  `momentum-tf-align` `momentum-nap-audit` `momentum-nap-audit-2`
  `momentum-pills-fpill` `sweep-2026-09-23` `sweep-2026-09-23-land`
  `turtle-job1-cleanup` `turtle-job2-nav-primary` `turtle-ui-pass`
  `turtle-money-readouts` `turtle-nav-home`
- throwaway diagnostics: `diag-els-4h-2026-09-23` `diag-sweep-2026-09-23`
- never merged, owner's call: `claude/optimistic-darwin-r6s49g`
  `claude/repository-overview-83qyz6`
- older: `audit-smooth-2026-08-23` `parity-pack`
  `claude/cloudflare-daily-limit-kn6ld1` `claude/how-you-go-m2wk8c`
  `claude/mobile-suburb-search-n04zwx` `claude/stalled-position-surface-8iiegn`

## V1 — leftovers

0 open PRs, so nothing to merge or close. No duplicate replays.

## V2 — production smoke: 76 / 76 PASS (09:12Z)

pages.dev is 403 from the sandbox, so it ran on a runner from the throwaway
branch `diag-validate-2026-09-23` (`diag/validate_smoke.js`, results +
screenshots in `diag/validate/` there), phone 390×844 unless noted.

| target | result |
|---|---|
| deploy skew | served `version.json` 2026.09.23-7ef6fd5d = repo; all 15 pages' `?v=` refs = repo; sw.js CACHE `vivek5-v8` = repo |
| `/` desk + phone | 5 `.fpill`, 72 A+ rows, no error, no overflow; pills `… SPECS ⚡ · MOMENTUM · ALERTS …` with MOMENTUM purple; phone TABS = 5 (index recs phasemap specs journal) + MORE; MORE sheet has momentum.html |
| `/momentum` (ASX default) | `MOMENTUM · 4 names` · mode A · 1-bar · 298 scanned · 867 gated · last bar 2026-09-22; 4 rows = served json; pills `.fpill` + `<b>`, no `.deck-pill`; links `chart.html?s=ELS&m=asx&src=momentum` |
| `/momentum?m=nasdaq` | 42 names = 42 rows = json; 1160 scanned · 268 gated; links `?s=…&src=momentum` |
| chart ELS `src=momentum` D | PLAN SHORT **5.880 / 6.762 / 4.998 / 4.116 / 3.234**, R:R 3.00 · price + MACD + RSI · 2354 daily bars · no SCORE 0/8 · no PhaseMap · back → momentum.html |
| chart ELS `src=momentum` 4H | **6.460 / 7.384** (TP 5.536 / 4.612 / 3.689) — not the Daily numbers · 3 panes · 912 4H bars |
| chart BHP, no src | SMA 10 / 20 / 43 (+200) · 5.0 footer family · no Pine caption, no Momentum panes · back → index.html |
| emptyState / fail() `src=momentum` | back → momentum.html; search lands on `?m=asx&s=ELS&src=momentum`; ZZQXJ → "Chart unavailable", back → momentum.html |
| /journal /alerts /phasemap /specs /recommendations | desk + phone: content, no page error, no overflow (5 `.rec-card`) |

No FAILs, so no V2 fix commits.

## V3 — full gate (local, this branch)

| gate | exit | detail |
|---|---|---|
| pytest | 0 | 1467 tests / 83 files (`/tmp/validate-pytest.exit`) |
| `node --check public/js/*.js` | 0 | 19 files |
| test.yml JS suites | 0 × 24 | every `test/*.test.js` (24) has a step (24); each own exit file |
| e2e smoke / hang-probe / screenshot matrix | 0 / 0 / 0 | ALL E2E CHECKS PASSED · 18 shots, zero overflow |
| test_version_stamp | 0 | digest `7ef6fd5d` from the test's own `fingerprint()` = version.json |
| test_momentum_fences (+ version stamp) | 0 | 25 passed |
| hygiene.test.js | 0 | 650 checks; re-adding `drawMiniEquity` (journal.js) or `.track` (styles.css) turns it red, restored byte-identical |

Not run locally: Lighthouse and screenshot-diff (need the CI baseline cache and
the `lighthouse` package); they run in the PR's e2e job.

## V5 — stale / skew

- **`?v=` vs bytes, per deploy.** Every consecutive pair of `data:` commits on
  main since 2026-09-09 (389 deploy points, 19 with a js/css byte change) was
  checked: wherever an asset's bytes changed, every page that loads it moved
  its `?v=`. One historical miss — `phasemap-shared.js` changed at
  `d19d6f746` (2026-09-20 09:06Z) still at `?v=16`, re-bumped by `5ac425b2a`
  2h later — nothing since. (Per-COMMIT the #36 sweep shows edits after its
  bumps, but it landed as one push, so no deploy ever served mixed bytes.)
- **Served vs repo:** production `version.json`, all 15 pages' `?v=` sets and
  sw.js CACHE match main exactly (V2).
- **This branch** bumps chart.js 124→125, sectors.js 12→13, styles.css
  117→118 (14 pages); version.json replayed from the test's `fingerprint()`
  each commit. **sw.js CACHE left at `vivek5-v8`**: `?v=` assets are
  cache-first under a new URL, so a bump is only for breaking changes — none
  here (the #36 sweep made the same call).
- **nav:** PRIMARY 7 → TABS 5 (`OFF_TAB` = alerts, momentum) + MORE; SHEET =
  momentum, alerts, news, system, how-it-works. MOMENTUM sits straight after
  SPECS and is purple (V2, and momentum.test.js pins both).
- **momentum.yml writes only `public/data/momentum/<market>.json`** — all four
  of its data commits touch exactly that one file.

## V4 — dead code (strict death test) + comments that lie

Candidates: ESLint `no-unused-vars` over public/js, a token-count scan of every
declared name / CSS selector / custom property / `_`-property across public,
test, tests, functions, scripts, scanner and workflows, then one "prove it
ALIVE" verifier per group. Deleted only what survived; one name or one linked
chain per commit, each pinned in `test/hygiene.test.js` and mutation-checked
(the previous file fails the new pin).

| deleted | why dead |
|---|---|
| chart.js `renderTFSetups(d, …)` param `d` | body never reads it; one caller |
| sectors.js `macroCardHTML(key, m)` param `key` | body reads only `m`; one caller |
| styles.css `--bg-grad` | no `var()` reader, no JS, no test |
| app.js `state.view` | no reader since the watch view went |
| chart.js `d._zonePlan` | assigned, never read (its reader, Simulate, went 2026-09-21) |
| chart.js `_momRow` / `_momParams` | stamped on the Momentum payload, never read |

Proven ALIVE and kept: app.js `syncPrefsUI` / `stickyToolbar` /
`purgeLegacyKeys` (named IIFEs — the body runs), `PM.fmtPrice` /
`PM.fmtTurnover` (destructured by momentum.js), `EYES.MAX_MARKS` /
`EYES.isDismissed` (eyes.test.js), and 19 CSS classes built at runtime
(`jr-mkt-${m}`, `jr-reason-${r}`, `w3-who-bot`, `vkf-*`, `ba-kind-${k}`,
`vk-${cls}`, `pm-density-${d}`, `.cl-item`, `.al-row`, `.nav-more-item`).

**Comments that lied**: ~140 found by nine read-only finders, each fixed after
checking the code; every JS commit proven comment-only by printing the file
with TypeScript (comments removed) before and after, every CSS commit by
comparing the comment-stripped text. `?v=` bumped per file, version.json
replayed each time. Two were **user-visible** and fixed as FAILs: the STATUS
sheet told the owner a 5-minute stop watcher runs (removed 2026-09-21), and
the welcome tour told new users to star names. `system.html` still documented
the manual journal, the cloud watcher, KV sync, a weekend crypto sweep and
"A+ is the only grade the bot trades" — corrected.

**KEEP + list (owner's call, not name-dead):**
- journal.js `renderEdgeTracker` / `renderLensTracker`: their hosts
  (`#edge-tracker`, `#lens-tracker`) left journal.html in `5ac425b2a` with
  the comparison overview, so **the Edge Tracker and Lens Tracker tables have
  not rendered since 2026-09-21** (system.html still points readers at the
  Edge Tracker). Restore the two hosts, or delete the renderers.
- journal.js `openMetric`'s non-bot branch + `scanPrice` (set in three places,
  read only there): unreachable since the Me book went; journal_money's
  `mePos` sort pins exercise it.
- app.js `.olw` lookup (always null) and the `.scan-btn` loops; specs.js /
  phasemap.js `state.view === "watchlist"` branches and their `_stale` badge
  (no tab or saved state selects them); `window.__gbsErrors` (console-only).
- the "+N" chip tooltip says "expand the row for every chip"; the VIVEK panel
  lists the scanner's `r.chips`, not the client badges.
- chart.js comments inside the cost-model block, the MACD/RSI panes and
  `makeLiveBoxDraggable` were left alone per the brief, even where stale.

## V6 — lying tests

- `drawEquity` is the live name (journal.js declares and calls it;
  journal_money pins the per-book call); `drawMiniEquity` stays pinned gone.
- statekeep: header + step name claimed #84/#88 and named `updateClosePreview`
  (gone); only #86 runs. Unread `JOURNAL_SRC` dropped.
- leaks: dead `atest` / `deferred` async queue removed (nothing registered).
- close_all: two pins sliced `paintOpen` up to a comment; now up to the next
  function.
- test.yml step names and prose in access_log, api_guards, close_all,
  journal_review, journal_money, journal_stale, lighthouse.e2e, staleview and
  cache named removed code or drifted counts (leaks "52" → 19, api_guards
  "17" → 28, momentum "82" → 388 …). Counts dropped where they drift.
- No python test names a public/js function that no longer exists.
