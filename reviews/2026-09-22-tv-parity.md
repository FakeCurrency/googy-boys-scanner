# Momentum chart — TV parity pass (2026-09-22)

`momentumPlan` untouched. ELS Daily still 5.8800 / 6.7620 / 4.9980 / 4.1160 /
3.2340 — within $0.01 of the screenshot, pinned by an executed test.

| # | before | after |
|---|---|---|
| P1 | opened on 2021, the live move squeezed into a fifth of the canvas | opens 2025-09-01, 254 bars — later of 12 months back and the end of the dead regime; pan still reaches all 1,265 |
| P2 | SL/Entry/TP drawn across 2024, on bars they were never measured against | two-point line series, signal bar → last bar only |
| P4 | axis could run below zero on a $5 name | price floor pinned at 0, momentum only |
| P5 | every cross in five years wore a text label | text inside the opening window, one per bar; older crosses keep the dot |
| P6 | caption chip on the candles, then on the volume strip | header chip |
| P7 | — | 390px: price 540px, two 127px panes, no h-overflow |
| P12 | `Rule A · bull divergence · trend up` | plan side badge + `Rule A bull` / `Rule B n/3` only |
| P13 | two same-bar divergences smashed into "BeaBear" | one tag per bar |
| P14 | — | already a right-hand column; ATH drops its label within 1.5% of SL |

**P3 NOT SHIPPED, and not faked.** The MA cloud needs a fill between two
*sloping* series. `lightweight-charts@4.1.3` offers `addBaselineSeries` with a
**constant** `baseValue: {type:"price"}`, and has no `addCustomSeries` (v5).
A draft was written and reverted: it would have put the green/red flip at a
fixed level instead of at the 20/200 crossover, i.e. coloured trend wrongly.
Options are a library upgrade or painting it on the existing `.draw-layer`
canvas. The palette half (20 blue, 50 orange, 200 green) shipped in #24.

**P8** ELS SHORT 5.880/6.762 · IPX SHORT 4.360/5.014 · BRE SHORT 4.510/5.187 ·
LRV SHORT 1.145/1.317. CTSH **unverified**: `data/history/` is ASX-only.
**P9** 4H uses the same hourly → `bucketBars(·, 4*3600)` path as the 5.0 chart.
Live 4H vs TV 6.46/7.39 **unverified: proxy** — no hourly fixture, Yahoo and
pages.dev blocked.

**Found by P10**, not by design: `fail()` and `emptyState()` rebuilt the header
with a hardcoded Dashboard link, so any chart that could not load data lost its
`src` and stranded the reader. Every probe I had stubbed the price feed, so
none had ever taken that path. Fixed for all modes.
