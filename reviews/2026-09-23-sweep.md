# Giant sweep — 2026-09-23

Dead code, audit, smooth. Branch `sweep-2026-09-23` off origin/main
`92e72d403`. **Nothing on the kill list was touched** (vivek.py, scan.py,
conviction.py, scanner/broker/**, journal/, confluence_alert.py,
morning_plays.py, phasemap/engine, bot_rules.json, scanner/momentum/screen.py),
and **`journal/` is byte-identical** on every commit (tree `d34876baf36d`).

## S0 — inventory

| item | value |
|---|---|
| open PRs at start | **0** (#19–#35 all rebase-merged; #35 = Momentum pills `.fpill`) |
| last code commit on main | `3c24d0979` momentum: filter pills are the deck's .fpill (2026-09-22 23:32Z) |
| last data commit on main | `92e72d403` data: scan 2026-09-23 02:30 UTC |
| `journal/` tree | `d34876baf36d` (unchanged by this branch) |
| TABS === 5 | yes — PRIMARY 7 minus OFF_TAB {alerts, momentum} (momentum.test.js pins it) |
| momentum in PRIMARY | yes, index 4, straight after SPECS (pinned by #34) |
| momentum in SHEET | yes (OFF_TAB keys + MORE) |

**Leftover remote branches (listed, not deleted).**
- Rebase-merged (content on main, SHAs not): `vivek-momentum-v1` (#19),
  `momentum-chart-link` (#20), `momentum-chart-mode` (#21), `momentum-tv-plan`
  (#22), `momentum-chart-agrade` (#23), `momentum-chart-tv-look` (#24),
  `momentum-overnight-2` (#25), `momentum-tv-parity` (#26),
  `momentum-list-parity` (#27), `momentum-drain` (#28),
  `momentum-morning-audit` (#29), `scanner-hygiene-2026-09-23` (#30),
  `momentum-phone-tf-bar-2026-09-23` (#31), `momentum-tf-align` (#32),
  `momentum-nap-audit` (#33), `momentum-nap-audit-2` (#34),
  `momentum-pills-fpill` (#35), `turtle-job1-cleanup`, `turtle-job2-nav-primary`,
  `turtle-ui-pass`, `turtle-money-readouts`, `turtle-nav-home` (lens removed).
- Throwaway diagnostics: `diag-els-4h-2026-09-23` (nap audit),
  `diag-sweep-2026-09-23` (this sweep's production smoke; see S1).
- Never merged, owner's call: `claude/optimistic-darwin-r6s49g` (source of
  `tradingview/Final_Top_Script.pine`), `claude/repository-overview-83qyz6`.
- Older, previously listed as merged ancestors: `audit-smooth-2026-08-23`,
  `claude/cloudflare-daily-limit-kn6ld1`, `claude/how-you-go-m2wk8c`,
  `claude/mobile-suburb-search-n04zwx`, `claude/stalled-position-surface-8iiegn`,
  `parity-pack`.
- `claude/beautiful-maxwell-idsvrz` = main, no extra commits (this session's
  harness branch).

## S1 — live smoke (production, read-only)

pages.dev, Yahoo and Binance all 403 from this sandbox, so the smoke ran on a
GitHub runner from the throwaway branch `diag-sweep-2026-09-23`
(`diag/sweep_audit.js`, results in `diag/sweep/results.json` there), against
`https://googy-boys-scanner.pages.dev` at 390×844 (deck also 1400).
**54 / 54 PASS** (run 2, 03:21Z). After the branch deployed, the same smoke (+ 3 checks that the restored chart.css skeleton / offline-banner rules are live) ran against the branch preview `sweep-2026-09-23.googy-boys-scanner.pages.dev`: **57 / 57 PASS**, ELS pins identical.

Production run:

| page | result |
|---|---|
| `/` deck, desktop + phone | title painted, 5 `.fpill` pills, 70 A+ cards, no overflow, no page error |
| `/momentum` ASX | 4 cards = header · pills `.fpill` + `<b>` (no `.deck-pill`) · every link `?s=…&m=asx&src=momentum` |
| `/momentum` NASDAQ | 42 cards = header · same pill/link checks |
| ELS `src=momentum` D | SHORT **5.880 / 6.762 / 4.998 / 4.116 / 3.234** (±$0.01) · 2 oscillator panes + price · 2354 daily bars · no SCORE 0/8 · no PhaseMap · back-link → momentum.html |
| ELS `src=momentum` 4H | **6.460 / 7.384** (TV 6.46 / 7.39, within $0.02) · not Daily's numbers · 3 panes · 912 4H bars |
| ELS `&pm=1` | loads with the plan, no error |
| BHP, no src | SMA 10 / 20 / 43 (+200) · 5.0 footer family (not the Momentum PLAN strip) · no Pine caption · no Momentum panes |
| AUB (on today's scan) | VIVEK footer proper (Setup · 200 SMA · Entry/SL/TP ladder) · SMA 10/20/43 |
| empty state `chart.html?src=momentum` | back-link → momentum.html · search keeps `src=momentum` |
| fail() `?s=ZZQXJ&src=momentum` | "Chart unavailable" · back-link → momentum.html |
| /journal /alerts /phasemap /specs | load, have content, no page error, no overflow |

Run 1 had one FAIL, and it was the check, not the site: BHP is not on today's
211-row ASX scan, so the chart takes the raw-chart path whose footer is the
generic 5.0 set (ENTRY/STOP/TARGET/TRAIL/SCORE/RISK/R:R). The check was
corrected to "5.0 footer family, not the Momentum PLAN strip", and a name that
IS on the scan (AUB) was added to prove the VIVEK footer proper.
**Observed, not changed:** that raw-chart footer prints `SCORE 0/0` and a green
`R:R 0.00` for a name with no setup — the same "row of blanks reads as broken"
problem the Momentum footer was fixed for. Owner call.

## S2 — dead code

Method: ESLint `no-unused-vars` + a comment-aware CSS selector scan → one
adversarial verifier per group (JS names, journal.css ×2, styles.css, other
CSS, namespace exports, a DOM-observation pass) told to prove each candidate
ALIVE → only verified-dead names deleted, one commit each, each pinned in
`test/hygiene.test.js` and mutation-checked (re-adding the name, or the name as
a quoted string, turns it red). Every CSS commit was pixel-diffed against
origin/main on all 19 pages (+ an opened deck row and opened journal folds) at
1400 and 390; everything stayed inside the main-vs-main noise floor.

**Deleted — JS** (public/js, −547 / +34 lines across js+css in total):

| file | name(s) | why dead |
|---|---|---|
| chart.js | `rec` param of `momentumFallback` | never read; caller keeps its own `rec` for `pmRec` |
| sectors.js | `dark()` | prefers-color-scheme helper nobody calls (site is dark-only) |
| journal.js | `statCards`'s `accent` param · `openRows`'s `isLong` local | never read / never passed |
| journal.js | `drawMiniEquity` | retired header sparkline; #33 moved the test pin to `drawEquity`, so nothing loads it now |
| journal.js | `today()` + the IIFE `pad()` only it used | journal_money's `today` is the test's own stub |
| app.js | `HEAD_PREFIX` / `HEAD_ROWS` aliases + the fallback object's two props | cache.js keeps its own copies |
| app.js | rowHtml's `sector` / `seccount` / `hasSectorCount` + `up()` | badge strings built and never rendered |
| phasemap-shared.js | `PM.fmtPct` / `PM.srcText` / `PM.zoneLabel` export entries | no reader (locals kept, still used inside) |
| eyes-store.js | `EYES.SEEN_KEY` / `EYES.CHAIN_KEY` export entries | no reader (vars kept) |
| status.js | `window.GBSStatus` "test hook" | its own comment said nothing read it; the suite slices the functions |

**Deleted — CSS** (markup nothing creates; pixel-identical within the noise floor):

| file | selectors |
|---|---|
| journal.css | 90 Me-side classes (mj-*, jr-tab*, cmp-*, lead-*, w-*/h-*, both-win, bsep, jr-actions/-btn/-cp-*/-modal-*/-price-*, jr-sync-pill, jr-pnl-*, jr-mini-eqsvg, dir-chip, hl, has-note, side-cta, reason, both, me) + `--me` |
| styles.css | 47: old two-row topbar (row1/row2, clocks, brand, nav, setups-title, scan-switch, nav-minor, pm-topnav), stars (t-star, starred, watched, tb-watch, tb-dim + `@keyframes star-pop`), vk-lvl* / vk-check*, rd-ema* / rd-spread / rd-tag, track, pulse, and orphans (accent-green, ba-label, control-label, deck-npick, grade-A/Ap, liq-*, olw-row, pmx, sort-arrow, badge.seccount), and three class rules that were really ids (vk-filters, bot-activity, topbar-quote) |
| phasemap.css | 20 + `#pm-chart`: old dense Specs rows (sp-row-main…), `.sp-list` (an id, never a class), pm-star, pm-tab-watchlist, the old chart page (pm-chart-head/box/note), pm-topnav, pm-more-btn |
| sectors.css | sec-table*, sec-sym, sec-name, sec-bar |
| stalled.css | st-arm (old two-click armed state), st-foot |
| status.css / about.css | sys-dim / ema-ladder |

**Repaired, not deleted — chart.css (a real production regression).** The
2026-09-20 MY JOURNAL removal (`5ac425b2a`) deleted rules with a pattern that
also ate two comment terminators, so each comment ran on to the next `*/`:
`.ct-market`, the whole `.chart-skeleton` loader + `sk-shimmer`, `.offline-banner`
and `.chart-error.is-offline` have been **unstyled on production for three
days** (offline notice as bare text over the chart; "Loading chart…" as a
plain line). CSS reports no parse error for it. Restored (css-tree: 215 → 241
live rules), with the orphaned `.ct-star` / `.sim-status` bodies dropped;
before/after screenshotted. Pinned as a CLASS: every stylesheet comment must
close before another `/*` opens.

**Kept on purpose** (per the brief, or because a test loads them):
`makeLiveBoxDraggable`, the chart.js cost-model block (`legCost` …),
chart.js `isHighConviction`; journal.js `costsFor`, `isVivek`, `markStale`,
`sizeOf`, `costR`, `RULES_GEN` (each sliced, stubbed or grepped by
journal_money / journal_stale — verified line by line); the named IIFEs.

**Kept because uncertain — listed for the owner:**
- `window.__gbsErrors` (telemetry.js): referenced by no code, but it is a
  documented devtools console accessor.
- `.scan-btn` (styles.css) **and** app.js's two `.scan-btn` loops: no page
  creates a `.scan-btn`, so both are inert — but removing the JS wiring is a
  behaviour edit, not a name deletion, and deleting only the CSS would leave
  the pair half-gone.
- `.tv-btn{` in chart.css: squashed by the same 2026-09-20 regex (it was
  `.tv-btn, .sim-btn {`); valid CSS, left as is.
- app.js's non-VIVEK detail renderers — `detailHtmlReversal`, `detailHtmlGoogy`,
  the default branch, and `heroStrip` / `metaBar` / `priceStrip` — plus the ~46
  styles.css classes only they emit (rd-*, dh-*, dl-*, fl-*, el-*,
  detail-hero, detail-meta, …). The DOM pass never saw them in 130 states: the
  deck's `<m>_vivek.json` rows always dispatch to `detailHtmlVivek`. But they
  are referenced by `fillDetail`'s `setup_type` dispatch, so under the strict
  rule they are not dead names; ruling the branches unreachable is a code
  decision. **The best-sized follow-up this sweep found.**

**Cross-checks.** The DOM-observation pass (a MutationObserver crawl: every
page at 1400 and 390, 130 states incl. stubbed charts, seeded storage and
route-substituted data that the producers can emit) saw 978 of 1,128 class
selectors on main. Every one of its 69 "never seen AND no literal creator"
classes is deleted above, and it never saw `reason` / `both` / `me` either.

## S3 — lying tests / stale comments

- `journal_money` "per-book equity curves": **confirmed fixed** (#33 re-pointed
  it at `drawEquity(pre + "-equity", …)`); `drawMiniEquity` itself is now
  deleted (S2).
- chart.js: the Momentum caption comment still said the chart has "no levels"
  and the caption says there are "none to load" — false since #22. Now names the
  Pine Auto plan. The PhaseMap-arrows comment is clarified the same way (no
  `levels` FIELD, because applyMomentumPlan draws the box). Comment-only: the
  espree token stream of chart.js is byte-identical to main for that commit.
- A read-only finder + per-finding re-check found **ten more** chart-path
  comments describing code that no longer exists ("only 1D carries a plan",
  panes "built with scaleMargins banding", PhaseMap zones "still ride along"
  ×2, "Daily first paint", the footer's "R each level sits at", the caption
  "pinned to the BOTTOM of the canvas", and two momentum.test.js headers). All
  corrected in one comment-only commit (token streams identical; css-tree
  regenerates chart.css to the same rules).
- **Lying test fixed:** momentum.test.js asserted `bucketBars(intraday, 4 * 3600)`
  as "4H is bucketed from hourly bars" — since the exchange-session change that
  matches only the crypto branch, so deleting the ASX/NASDAQ session path would
  have passed it. It now pins the whole two-branch line; the mutation that drops
  the session branch turns it red (it passed before).
- **Nine more lying tests fixed**, found by a tracer that hooked every
  RegExp/String/`in` check the JS and Python suites run against shipped
  sources and re-evaluated it with comments blanked. Each was REPRODUCED here
  (the mutation passes the old test) and re-checked after the fix (it passes
  on shipped code and fails under the mutation). One commit each:

  | test | lie | mutation it let through |
  |---|---|---|
  | escaping (#75 XSS pin) | checked the first `data-countdown` line = a comment | removing `esc()` from sectors.js's real template |
  | eyes | counted `renderEyes()` (decl + comment + handlers) | deleting the confluence-load call |
  | test_funnel_history (lazy fetch) | the IIFE's own `=> {` satisfied it | an eager page-load fetch of the 1/3 MB ledger |
  | test_funnel_history (staging) | crypto_bot.yml's YAML comment names the path | dropping it from `PATHS="…"` |
  | staleview (honesty capture) | the "5y" regex saw only crypto calls | stripping the capture flag from every stock pull |
  | journal_stale | `.jr-stale` named in a comment | deleting the `.jr-stale {}` rule |
  | test_specgrad | the path named in a comment | removing specs.js's fetch |
  | test_backtest_truth | a comment dragged journal.js (live book) into scope | (false RED: a legit live-book `total_usd` failed) |
  | leaks | message named the retired duration timer | (message only) |

  **Left as found, on purpose:** deep_history.test.js's `/CHART_MAX_YEARS/`
  also matches a doc comment in functions/api/price.js (fix: `/env\.CHART_MAX_YEARS/`)
  — CHART_MAX_YEARS is on this sweep's forbidden list, so it is reported, not
  touched.
- **Follow-ups queued as task cards, not changed here** (behaviour, not
  comments): the 4H/3D tooltip "trade levels are the Daily plan" shows on
  Momentum charts too; `?pm=1` drops the scored-cross labels (applyPmZones
  re-seeds from Rule A marks only — the comment now says so); the ⧉ Plan
  button can copy the Daily levels under a stale TF label; the header direction
  badge does not follow a TF switch; the off-scan / Rule-B-only analysis line
  says "raw chart" / "RSI divergence" when neither is what is drawn.

## S4 — version / nav / cron

- **test_version_stamp green at every commit.** Each asset edit bumped its `?v=`
  on every page that loads it, and `public/version.json` was rewritten from the
  digest the test itself prints (scratch `stamp.sh`), never guessed.
- **nav.js** (read by evaluating its own arrays): PRIMARY = index · recommendations ·
  phasemap · specs · **momentum** · alerts · journal; OFF_TAB {alerts, momentum}
  → **TABS = 5**; SHEET = OFF_TAB keys + MORE, so momentum is on the phone sheet.
  Untouched.
- **momentum.yml**: own `concurrency: group: momentum` (not `scan`); stages exactly
  `public/data/momentum/$MARKET.json`, one pathspec, `assert_staged`; the retry
  loop's `git add -A` runs after a hard reset + checkout of this run's own file
  only. Untouched.
- **Runs**: only 3 ever — two manual dispatches (22 Sep 07:19Z, 07:31Z) and the
  first scheduled NASDAQ run (21:30Z cron, started 23:42Z, ~2h12m late). The
  00:30Z crypto cron had not fired by 04:30Z on the 23rd and there is **no
  `crypto.json` yet**; the first scheduled ASX run is 06:30Z today.
- **ASX coverage is low: 1165 of 2047 downloaded (57%)** on the only ASX file
  (the cold-cache manual run, `reused: 0`) → 298 scanned, 867 gated, 4 hits.
  NASDAQ 1428 / 1428. Yahoo, gates and screen.py unchanged — read the next
  scheduled ASX commit's `summary.downloaded` before drawing a conclusion.

## S6 — drain

- **Test fixtures:** none unused. `test/fixtures/momentum/{ELS.AX,AAPL}_1h.json`
  are read by momentum.test.js; all nine `test/e2e/fixtures/data/*.json` are
  fetched as `/data/` by some page (fx.json by journal.js, the `_detail` files by
  app.js per market) and are hashed into the screenshot cache key.
- **Reviews older than 14 days:** none in `reviews/` (all are 09-22 / 09-23).
  Root-level docs dated by name before 09-09, listed only, not touched:
  PROPOSAL-2026-07-29, CONFLUENCE-REVIEW-2026-08-01, EVIDENCE-INTEL-2026-08-01,
  PAPER-BOOK-REVIEW-2026-08-01, BATCH100_2026-08-20, EDGE_RESEARCH_2026-08-20,
  HANDOFF_2026-08-21, docs/MEMO_2026-07-28_sector_rotation. Several still describe
  HORIZON / REGIME / TURTLE / the manual journal as live — history, not law.
- **Scratch:** nothing untracked in the repo tree. The two scratch worktrees this
  sweep made (origin/main baseline + the diag branch) were under the session
  scratchpad and are removed.

## S5 — landed: #36 → main `9063f3d95`

- Rebase-merged with all four checks green on the head (javascript, python,
  e2e, Cloudflare Pages); 37 commits, 26 code/test files, −609 / +562 lines.
- **Deleted names:** chart.js `rec` param · sectors.js `dark` · journal.js
  `accent`, `isLong`, `drawMiniEquity`, `today`, `pad` · app.js `HEAD_PREFIX`,
  `HEAD_ROWS`, `sector`, `seccount`, `hasSectorCount`, `up` · exports
  `PM.fmtPct/srcText/zoneLabel`, `EYES.SEEN_KEY/CHAIN_KEY`, `window.GBSStatus` ·
  ~170 CSS selectors across 7 stylesheets (tables above). All pinned in
  test/hygiene.test.js.
- **FAILs fixed:** none from the smoke (54/54). Found and fixed on the way: the
  chart.css comment regression (skeleton / market chip / offline banner
  unstyled on production since 2026-09-20), 9 lying tests, 11 stale comments.
- **Local gate at the final code commit:** 24/24 registered JS suites, pytest
  1467 passed, e2e smoke 29/29, screenshot-diff 0.00% ×4 — exit codes read
  from files.
- **Post-merge production smoke (05:03Z): 57/57 PASS**, serving the merged
  stamp `2026.09.23-7ef6fd5d`. **ELS pins still hold:** Daily 5.880 / 6.762 /
  4.998 / 4.116 / 3.234, 4H 6.460 / 7.384; the restored skeleton and offline
  banner are styled live.
- **`journal/` not staged:** no commit in #36 touches it (the one journal
  change on main since the base is the bot's `data: scan 04:45 UTC`).
